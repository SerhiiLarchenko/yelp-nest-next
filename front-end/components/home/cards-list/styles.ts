import styled from 'styled-components';

const StyledCardsList = styled.ul`
  width: 360px;
  position: fixed;
  overflow-y: scroll;
  overflow-x: hidden;
  top: 96px;
  bottom: 0;
`;

export { StyledCardsList };
