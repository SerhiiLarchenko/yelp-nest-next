const white = '#fafafa';
const black = '#040c35;';
const standardRed = '#ce464d';
const grey = '#72758a';

const text = { basic: black, secondary: grey };

const border = {
  card: {
    default: '#e8e8e8',
    hover: '#cdcdcd',
  },
};

const COLORS = {
  primary: standardRed,
  text,
  background: white,
  border,
};

export { COLORS };
